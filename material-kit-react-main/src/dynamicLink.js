// dynamicLink.js
const getDynamicLink = () => {
    // Provide the logic to generate the dynamic link here
    return `http://$/users/`;
  };
  
  // Set the global variable for external access
  window.dynamicLink = getDynamicLink();
  